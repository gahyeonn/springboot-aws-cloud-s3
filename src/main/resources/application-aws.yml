cloud:
  aws:
    s3:
      bucket: # AWS S3 bucket name
    region:
      static: ap-northeast-2
      stack:
        auto: false
      credentials:
        instanceProfile: true #AWS CLI에서 AWS configure list 정보 반영 여부